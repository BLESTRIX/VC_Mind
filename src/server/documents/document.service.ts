import { randomUUID } from 'node:crypto';
import { getEnv } from '../../lib/env.js';import { AppError } from '../../lib/errors.js';import { sha256 } from '../../lib/hashing.js';import type { AuthContext } from '../auth.js';import { assertApplicationAccess,requireRoles } from '../auth.js';import { getServiceClient } from '../supabase.js';import { validatePdf,type PdfFile } from './file-validator.js';import { extractPdf,type ExtractedDocument } from './pdf-extractor.js';
export class DocumentService{
 async uploadPitchDeck(applicationId:string,file:PdfFile,auth:AuthContext){requireRoles(auth,['admin','investment_manager','analyst']);await assertApplicationAccess(auth,applicationId);const env=getEnv();validatePdf(file,env.MAX_PDF_SIZE_MB*1024*1024);const hash=sha256(file.buffer);const db=getServiceClient();const {data:duplicate}=await db.from('documents').select('*').eq('application_id',applicationId).eq('sha256_hash',hash).maybeSingle();if(duplicate)return duplicate;const id=randomUUID();const path=`applications/${applicationId}/documents/${id}/v1.pdf`;const {error:uploadError}=await db.storage.from(env.SUPABASE_STORAGE_BUCKET).upload(path,file.buffer,{contentType:'application/pdf',upsert:false});if(uploadError)throw new AppError('DOCUMENT_PROCESSING_FAILED','Private pitch-deck upload failed',500,{storage:uploadError.message});const {data,error}=await db.from('documents').insert({id,application_id:applicationId,uploaded_by:auth.userId,document_type:'pitch_deck',storage_bucket:env.SUPABASE_STORAGE_BUCKET,storage_path:path,original_filename:file.filename,mime_type:file.mimetype,file_size_bytes:file.buffer.length,sha256_hash:hash,processing_status:'pending'}).select('*').single();if(error){await db.storage.from(env.SUPABASE_STORAGE_BUCKET).remove([path]);throw new AppError('INTERNAL_ERROR','Document metadata could not be stored',500,{database:error.message});}return data;}
 async extract(documentId:string):Promise<ExtractedDocument>{const db=getServiceClient();const {data:doc,error}=await db.from('documents').select('*').eq('id',documentId).single();if(error||!doc)throw new AppError('NOT_FOUND','Document not found',404);if(doc.processing_status==='completed'){const {data:pages}=await db.from('document_pages').select('*').eq('document_id',documentId).order('page_number');return{documentId,pages:(pages??[]).map((p)=>({pageNumber:p.page_number,text:p.page_text??'',textHash:p.text_hash??sha256(p.page_text??'')})),pageCount:doc.page_count??0,characterCount:(pages??[]).reduce((n,p)=>n+(p.page_text?.length??0),0),warnings:[]};}await db.from('documents').update({processing_status:'processing'}).eq('id',documentId);try{const {data,error:downloadError}=await db.storage.from(doc.storage_bucket).download(doc.storage_path);if(downloadError||!data)throw new AppError('DOCUMENT_PROCESSING_FAILED','Stored PDF could not be downloaded',500);const result=await extractPdf(documentId,new Uint8Array(await data.arrayBuffer()),getEnv().MAX_PDF_PAGES);await db.from('document_pages').upsert(result.pages.map((p)=>({document_id:documentId,page_number:p.pageNumber,page_text:p.text,text_hash:p.textHash})),{onConflict:'document_id,page_number'});await db.from('documents').update({page_count:result.pageCount,processing_status:result.characterCount<100?'failed':'completed',processed_at:new Date().toISOString(),extracted_text:{characterCount:result.characterCount,warnings:result.warnings}}).eq('id',documentId);if(result.characterCount<100)throw new AppError('DOCUMENT_PROCESSING_FAILED','PDF appears image-only; submit a text-searchable PDF',422,{warnings:result.warnings});return result;}catch(error){await db.from('documents').update({processing_status:'failed',processed_at:new Date().toISOString()}).eq('id',documentId);throw error;}}
}
