import{describe,expect,it}from'vitest';import{normalizeUrl}from'../../search/result-normalizer.js';import{sourceQuality}from'../../server/evidence/source-quality.js';import{evidenceConfidence}from'../../server/evidence/evidence-confidence.js';
describe('evidence logic',()=>{it('normalizes URL domains and tracking',()=>expect(normalizeUrl('HTTPS://WWW.Example.com/a/?utm_source=x#y')).toEqual({url:'https://example.com/a',domain:'example.com'}));it('rejects dangerous protocols',()=>expect(()=>normalizeUrl('javascript:alert(1)')).toThrow());it('discounts founder-controlled evidence',()=>expect(sourceQuality({type:'company_website',founderControlled:true})).toBeLessThan(sourceQuality({type:'company_website'})));it('does not double-count one cluster at full strength',()=>{const one=evidenceConfidence([{sourceQuality:.8,entityMatch:1,completeness:1,relationship:'supports',independenceCluster:'a'}]);const copied=evidenceConfidence([{sourceQuality:.8,entityMatch:1,completeness:1,relationship:'supports',independenceCluster:'a'},{sourceQuality:.7,entityMatch:1,completeness:1,relationship:'supports',independenceCluster:'a'}]);expect(copied).toBe(one)});it('context-only evidence provides no coverage confidence',()=>expect(evidenceConfidence([{sourceQuality:1,entityMatch:1,completeness:1,relationship:'context_only'}])).toBe(0));});
