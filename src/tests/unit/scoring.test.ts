import{describe,expect,it}from'vitest';import{SCORE_WEIGHTS,validateWeights}from'../../server/scoring/scoring.config.js';import{overallScore,rubricTotal}from'../../server/scoring/scoring.service.js';import{recommend}from'../../server/scoring/recommendation.service.js';
const scores={founder:8,market:8,traction:8,product:8,thesis_fit:8,deal_economics:8,risk_resilience:8};
describe('deterministic scoring',()=>{it('weights total exactly one',()=>{expect(()=>validateWeights()).not.toThrow();expect(Object.values(SCORE_WEIGHTS).reduce((a,b)=>a+b,0)).toBe(1)});it('calculates the weighted overall score',()=>expect(overallScore(scores)).toBeCloseTo(8));it('bounds rubric scores',()=>expect(rubricTotal({a:3,b:-1,c:2})).toBe(4));it('passes a hard thesis failure',()=>expect(recommend({scores,overall:9,evidenceCoverage:100,hardThesisFailure:true,criticalContradiction:false,unresolvedBlockingRisk:false,unacceptableDealEconomics:false,materialDataInconsistency:false,incompleteCriticalDiligence:false,missingPrivateTractionValidation:false,missingDealTerms:false}).recommendation).toBe('pass'));it('requires information when coverage is low',()=>expect(recommend({scores,overall:8,evidenceCoverage:20,hardThesisFailure:false,criticalContradiction:false,unresolvedBlockingRisk:false,unacceptableDealEconomics:false,materialDataInconsistency:false,incompleteCriticalDiligence:false,missingPrivateTractionValidation:false,missingDealTerms:false}).recommendation).toBe('needs_more_info'));});
