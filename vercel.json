{
  "version": 2,
  "buildCommand": "if [ -f frontend/package.json ]; then cd frontend && npm install && npm run build && rm -rf ../.output && mkdir -p ../.output && cp -R .output/public ../.output/; else npm install && npm run build; fi",
  "outputDirectory": ".output/public",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...path]"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}